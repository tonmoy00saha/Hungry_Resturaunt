import { Helmet } from 'react-helmet-async';

const Order = () => {
    return (
        <div>
            <Helmet>
                <title>Hungry | Order Food</title>
            </Helmet>
            <h2>Order</h2>
        </div>
    );
};

export default Order;